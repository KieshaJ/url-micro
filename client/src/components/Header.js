import React from 'react';

const HeaderComponent = () => {
    return(
        <header>
            <h1>URL Micro - Generate short urls in seconds!</h1>
        </header>
    );
};

export default HeaderComponent;